package bkxss;

/** Provides a non-JavaFX entry point for launching the graphical application. */
public class Launcher {
    /** Starts the Bkxss graphical user interface. */
    public static void main(String[] args) {
        BkxssGui.launch(BkxssGui.class, args);
    }
}
